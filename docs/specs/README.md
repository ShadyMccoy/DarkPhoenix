# Task Specs

Detailed, self-contained specifications for the next tranche of work. Each spec
defines its goal, design, file-level changes, and — most importantly — the
**acceptance tests**: a task is DONE exactly when its listed tests pass (and
nothing previously green breaks). Write the tests first; they are the contract.

**The keystone is [spec 00 — the Corp Framework](00-corp-framework.md):** corps
as interchangeable units behind an abstract input/output interface, so the
planner reasons over an abstracted world and new corp kinds plug in without
touching the core. Specs 02, 03, 06 and 07 should be implemented as corp kinds
through that framework; they double as proof that it works.

Conventions used by every spec:

- **Unit tests** run via `npm run test-unit` (mocha, mocked Game API, ~seconds).
- **Integration tests** run one file at a time via
  `npx mocha "test/integration/<file>.test.ts"` (screeps-server-mockup, real
  engine, minutes). Always `npm run build` first — the engine runs
  `dist/main.js`, not your working tree (a stale bundle cost this project a
  debugging hour already).
- **Regression gate** for any task touching live behavior: `npm run test-unit`,
  plus the `flow-handoff`, `runt-economy`, and `storage-depot` integration
  tests, all green.
- Diag probes (`scripts/diag-*.ts`) are for investigation, not acceptance.
  Acceptance criteria live in tests only.

| # | Spec | Status | Priority |
|---|------|--------|----------|
| 00 | [Corp Framework](00-corp-framework.md) | **COMPLETE** — all kinds run through CommissionHost; polish notes remain in-spec | landed |
| 01 | [Early-game progression](01-rcl5-cold-start-stall.md) | mostly closed (cold-start pipeline pinned by resilience cells); remaining question: live RCL2-3 throughput | P1 |
| 02 | [Link logistics](02-link-logistics.md) | shipped in altered form: LinkRunner (free function) + planner haulPos pricing; the corp-kind redesign remains open | P2 |
| 03 | [Storage draw-down](03-storage-drawdown.md) | LANDED — deposit half, controller feeder relay, and planner-level bank-as-supply (transient bank sources + hub-and-spoke) all live | P1 |
| 04 | [Retire the chain/market layer](04-retire-chain-layer.md) | **DONE 2026-07-12** — `economy/siteValue` re-bases node/spawn valuation on planColony; ChainEvaluator/ColonyEconomy deleted | landed |
| 05 | [Toolchain upgrade](05-toolchain-upgrade.md) | approved, not started | P2 |
| 06 | [Expansion: claim the next room](06-expansion.md) | **LANDED 2026-07-10** — capital-gated claiming + sink-based founding, exp-t5 cells green. exp-t5-founding-funnels-to-completion red since the refactor era (full campaign failure) — **PARKED, owner 2026-07-28**: expansion is out of the current fidelity program's scope; baseline keeps the ratchet claim so the regression stays visible | landed (founding red parked) |
| 07 | [Tower defense (minimal)](07-tower-defense.md) | **LANDED 2026-07-17** (spec 13 tranche 1) — TowerRunner + RCL3 placement + tender feeding; tower-defense integration test green | landed |
| 08 | [Inflection-point grid](08-inflection-grid.md) | ~130 cells; BOT LEVEL ratcheted. 2026-07-28 close-out: BOTH #143 "master regressions" resolved as test artifacts (groundpile — staging gap; link-haul-pricing — re-pinned to the spec-02 haul-of-zero contract it predated); refill-SLA class resolved by rider precision (release carve-out + de-minimis, owner ruling); accepted reds: exp-t5-founding (PARKED, out of program scope). **plan-t5-remote-pipeline #148 regression FIXED 2026-08-03** (phase-3 haul work, as owed): the probe-traced root was FIVE stacked latency sources in the depot-less refill regime under #148's smaller/faster spawn cadence — (1) a builder parked on the spawn drop tile hoovering deliveries while the bank was short (fixed: `isSpawnRefillStock` claim-order guard, the sink ladder applied at the STOCK level, construction fuel paths yield within 2 of a spawn while the bank is short); (2) the tender's 15-tile pile reload (fixed: spawn store joins the reload ladder, ≥50 floor after (3) the regen-trickle trap it initially introduced); (4) one 7-carry body vs a 400e extension wave (fixed: depot-less one-wave fleet floor in `tenderFleetTarget` — the v1 one-trip rule at its correct scope; the t72663189 over-provision pin holds); (5) two tenders bus-bunching on one circuit (fixed: even tour-offset seeding). builder-buffer-feed passes standalone (host-load class); **plan-t2-antidowngrade-construction and fid-t4-synthetic-steady-state are PRE-EXISTING reds** (2026-08-03/04 bisects acquit the refill/drain changes; filed in spec 14) — baseline ratchet debt, windowed with the plan-t5/cons-t3/plan-t1/spawn-timer set | ongoing — the success metric |
| 09 | [Robustness program](09-robustness-program.md) | phases 1, 4 done; 5 partial (CpuGovernor + bulkheads; schema versioning open); 6 partial (standdown); 2-3 open | P0 (phase 2) |
| 10 | [RCL journey map](10-rcl-journey.md) | living ledger — most steps cell-pinned; see gap list | ongoing |
| 11 | [Two plans: goal and now](11-two-plans.md) | phases 1-2 landed (agenda published + funding); phase 3 (transitions into agenda) open | P0 |
| 12 | [Invader protocols: flight, and eventually fight](12-invader-protocols.md) | phase 1 (flight) **LANDED 2026-07-16**, stays as the fallback layer; phase 2 **LANDED 2026-07-17** via spec 13's CoreBusterCorp (kill + strip, corrected math), def-t5-core-buster cell green | landed |
| 13 | [Invader economics: keep the remotes flowing](13-invader-economics.md) | **LANDED 2026-07-17** — scout-wipe fix, towers, raid meter + transit embargo, RaidGuardCorp (def-t4 green), CoreBusterCorp (def-t5 green), invader tax, BlackBox/intel telemetry; open: live tax calibration = the R1 swap protocol (2026-08-05 state: ≥10 accumulated fiscal windows required, v32 `hostileAtDeath` counters accumulating the sighting-lag evidence; steady reads run ~0.6-0.7x the priced tax — mildly over-taxed; NEVER swap on a short-ring read, the 121t post-reset window printed 26x from ramp bodies) | landed (R1 swap gated on windows) |
| 14 | [Telemetry observability: answer the basic questions](14-telemetry-observability.md) | ALL PHASES implemented 2026-07-18 (bodies #111/#113, ledger+sizing #114, gate stamps, spawn meter + NOW-plan mirror core-v5); found+fixed the live reserver purchase loop. **Since then: the living audit-cycle log and the registration home for standing experiments** — currently P-55 (marginal-source accretion: controller+bank ≥ 53 post-repricing, J-curve reclass gate ARMED by the W41N25 admission → controller+bank+construction ≥ 55), the handicap lift (SPAWN_PLAN_FRACTION 1.0, prod control = 3 pre-lift windows, reversion criteria on the constant), and the MINING_BUDGET_FRACTION 0.6 audit (unblocked post-global-admission, not yet run). Read the tail before starting any new cycle | P1 |
| 15 | [Waste ledger: make every leak a measured number](15-waste-ledger.md) | phase 1 LANDED — `npm run audit:ledger` drives every audit cycle (P1-P9, E-class, S3, X-class rows); found the collapse-era leaks same-day 2026-07-22; gauge set now spans **methodology #11** (budget column balances by construction, bank = plan residual; F1–F3, L1, SCAV, DEP, LINK, RECOVERY P&L, the v31 pile census, balance sheet, P12 at the phase-D law). Corps v16 adds the hauler DepartMeter (departure reason + cargo frac per corp — one capture dissolved the cd94 "anomaly" 2026-08-05). Standing programs the gauges carry: R1 raid-tax swap (≥10 fiscal windows + v32 hostileAtDeath evidence gate — ring-artifact reads like 26x on a 121t window are NOT the signal), the 3.34 e/t parts-vs-energy internal gap (phase-1 ratio-aware ENERGY repricing still open; admission side closed 2026-08-05), cbd8 P&L watch (post-paved-repricing honest worst row). Open: S3 executed-head polish, E5 scavenge-vs-route labeling | P0 |
| 16 | [Construction as projects](16-construction-projects.md) | proposed 2026-07-19 (owner design) — a builder corp is a spawn + a finite-cost project list; cross-room remote-source trunk paving + sum-of-projects sizing landed; founding-as-project forward-looking | P1 |
| 17 | [Ontology layers](17-ontology-layers.md) | IN PROGRESS 2026-07-20 — P0-P2 + enforcement landed (registration-only kinds, prescriptive NOW plan, purity ratchet); P3 (propose purity/host problem), P4 (envelope honesty), P5 (dead code) open | P0 |
| 18 | [Strategy: weighted goals + supply-chain search](18-weighted-goals.md) | PROPOSED 2026-07-20 (owner strategy thesis, revised) — the chain STRUCTURE is the searched decision (event-triggered, transition-costed), planColony is the evaluator, the NOW plan the transition executor; node grain not room grain; warfare priced as economics | P1 |
| 19 | [Delivery contract](19-delivery-contract.md) | PROPOSED 2026-07-20 (owner design) — spawning delivers newborns to each corp's declared delivery location; work functions assume on-post; creeps-as-cargo deferred | P2 |
| 21 | [Conquest](21-conquest.md) | PROPOSED 2026-07-20 (owner doctrine) — peace as the default strategy; conquest a narrow economic exception: probe→assess→harass→siege→claim ladder against measured-weak owners of desirable rooms when claims are scarce, with a pre-committed abort rule | P3 |
| 20 | [Corp accounting](20-corp-accounting.md) | phases 1-2 LANDED (1: per-corp CPU metering at the pure dispatch seam, Memory.corpCpu ledger; 2: the residual NAMED — bulkheaded infrastructure buckets + wholeTick reconciliation live in main.ts, shipped with #136's ledger cache); phase 3 open: migrate towers/links/bootstrap/spawning to kinds | P1 |
| 25 | [Emergent dedication](25-emergent-dedication.md) | **LANDED + VERIFIED LIVE 2026-07-22** — role-rule refinement + pool-room sink admission + poolAllocatedRate crew sizing; measured 30 e/t dark-dedicated → 70/70 routed after phase 3 | landed |
| 26 | [Links as hub ports](26-links-as-hub-ports.md) | STAGE 4 LIVE 2026-07-24 — source-link deposit ports shipped + verified (staffed core drain; toHub 8.5→18.6, directShare 0); controller-link ports still deferred (v2, need relay drop-reserve). Stage 5 (link PLACEMENT as an L/throughput optimization) + storage↔controller merge banked in-spec. **OPEN P7 FINDING 2026-08-03 (post-spec-38 windows, t72744219→t72744628): the controller leg ships 17.8 e/t against a 58 e/t plan** — GCL confirms burn 18.0/t with 57 WORK standing; feeder delivered-counter 55.8 spans BOTH its legs; link ledger: toHub 62.2, toControllerRate 17.8 (was 42.3 in the M05 era), hubVolleyAvg 447, hubClampShare 0.44. Burn == link arrival to the decimal, so the chain sits in a LOW equilibrium: relay volleys clamp to what the upgraders drank since the last fire. TWO live hypotheses the stale upgradeMeter cannot yet split (meterTicks 3225 spans the deploy — its workUtil 0.999/X1 0.10 are pre-deploy-weighted): (a) upgraders idle ~70% post-deploy (meter will show it once the window rolls); (b) the draw path physically caps (~4 fleet CARRY parts / input-spot election / adjacency). **RECOVERED by 2026-08-04 without a dedicated fix** — M09's link ledger reads toController 73.7 e/t against the 17.8 finding; the 08-03/04 allocation-continuity + feeder stretch closed the low equilibrium. Standing residue: the upgradeMeter reset-roll item. (Superseded next-steps kept for archaeology:) NEXT: fresh-window meter read + input-spot election check; candidate seams already mapped — coreLinkTargetLevel/coreLinkDrainAmount (the feeder-vs-relay race for core energy), LinkRunner's relay gate, controllerInputSpot election — **those seams are now SPEC 45's measured program (2026-08-05): the feeder-vs-relay race and the relay gate were caught in the act on the DEPOSIT side (coreEmptyShare 0.26, hubClampShare 0.50)**. ALSO queue: upgradeMeter must roll its window on global reset (a meter spanning a deploy poisons X1/workUtil for the whole acceptance read — spec 45's live acceptance must read post-roll windows) | P2 (diagnosis absorbed by 45) |
| 27 | [Extension relocation](27-extension-relocation.md) | PROPOSED 2026-07-22 (owner) — **next session's first work item**: phase-1 scorer + per-cluster table (size, distance, implied tender body), owner reviews before any destroy; folds in per-cluster tender sizing + the depot bridge economy fix (bus-regime red) | P0 |
| 28 | [Source-Keeper mining](28-source-keeper-mining.md) | design overview only — not started (guarded-producer model + garrison tax primitive + KeeperGuardCorp) | P3 |
| 29 | [CPU as a costed resource](29-cpu-as-costed-resource.md) | stub — deferred until CPU is the binding constraint (planner objective prices spawn-build-time + energy, not CPU) | P4 |
| 52 | [Base layout: highways + serpentine string](52-base-layout-serpentine.md) | PROPOSED 2026-07-25 (owner session) — design tooling landed (`scripts/base-lab` + extension-sim); measured across 13-74% wall (one lane tender, util 1.000). Target shape for spec 27; live graduation (tender-aware core placement, serpentine placement in ConstructionCorp, lane tender) is the work | P2 |
| 30 | [RaidingCorp: haul the loot out of soft targets](30-raiding.md) | PROPOSED 2026-07-23 (owner backlog) — grab-and-go loot hauling from **derelict bots** (full store + a dark tower they can't refill) as a guarded transient source (scavenge + military exemption); the **probe is the feature** (weakness as a trend, not a snapshot), hauling is straightforward; blocked on typed resources (loot is mostly non-energy) and new "easy-pickings" intel (enemy store contents + tower/decay state unrecorded today) | P3 |
| 31 | [Lab reaction network](31-lab-reaction-network.md) | DESIGN NOTE 2026-07-25 (owner idea) — the **circular lab layout** that reads reactants across cooldown: a lab on cooldown can still be a reactant, so the usual 2-idle-feeder layout wastes throughput; a ring where every lab both produces and feeds neighbours recovers it. Unranked — labs are unmodeled today; downstream of a mineral producer class (spec 28 tail). Throughput win is the open work; all constants pending engine-verify | unranked |
| 32 | [Graceful mining backoff on a pile](32-graceful-mining-backoff.md) | BACKLOG 2026-07-26 (owner idea) — deprioritize miner/reserver SUCCESSOR spawns when a source pile stands high (fail-graceful: stop mining energy that decays on the ground). Priced at the spawn, never revokes standing assets. HARD prerequisite **DISCHARGED 2026-08-02** (income-statement program phase 2): per-source measured inflow (`produced`) + per-operation landed energy (`delivered`) publish in corps segment v14, so surplus vs under-delivery is a capture read; the knob itself stays gated on the anti-sweep grid cells (backoff-surplus / backoff-underdelivery) and ships only if the soak shows a genuine surplus branch | P3 |
| 33 | [Wartime (build) economy](33-wartime-build-economy.md) | **LANDED + FULL ARC VERIFIED LIVE 2026-07-27** — acceleration + tanker right-size + upgrader-FLEET relegation (the plan-side cap was falsified as a physical no-op; the fleet lever worked: P7 18.8→9.8, build 0→3.91 e/t, extensions REBUILT, clean exit + re-expansion measured t72601582/t72601836). Open: entry/exit hysteresis as a named posture; the `wartime-build-*` grid cell | P2 |
| 34 | [Operation corps: the corp as economic interface](34-operation-corps.md) | **ACTIVE 2026-07-27** (owner thesis) — simple economic interface up (positions/rates/ALL-IN price), sophistication inside, faithfulness measured. LANDED: buffer/vector/crossover primitives, parked buffered builder (D1-D3b incl. unladen relocation), all-in construction price (D4), D6 cohort release at confirmed operation end, `builder-buffer-feed` cell green (91-92% workUtil), **D5 second half 2026-07-28: the MINER OPERATION** (one commission per mined source; haulers the harvest kind's internal squad), and the **2026-07-28 close-out tranche**: P4 ledger consistency landed, the refill-SLA class closed by owner ruling, the vector-gait tension RESOLVED. OPEN: item 1 (infra honesty pass — feeder/tender/scout/reserver still declare spawnPartsPerTick 0), item 5 (planner-owned dedication, UNBLOCKED 2026-08-02: owner ruled BOUNDED PRE-PASS - one nearest source claimed outside the ladder, no value nudges), and the two vector-gait follow-ups — **B LANDED 2026-08-02** (income-statement phase 1: `roadEconomics.vectorSupplyPartsGait`, the commission's all-in price now matches the 3:1 fleet the runtime fields; `tankerCarryNeededFor` migrated to the formula home), A (staged-roads grid cell) still open | P1 |
| 36 | [Post-refactor unlocks](36-post-refactor-unlocks.md) | PROPOSED 2026-07-27 (session follow-up to spec 35) — the ranked behavioral backlog the refactor made cheap: event-triggered replanning via runPlanningPhase(force), the haul-t4 depot-bridge fix in haulPolicy, the backoff instrument segment (gates master's spec 32), new kinds off the cheap contract (KeeperGuard, minerCorp, link kind), what-if solves at the assembly seam, placement work against the pure ladder, structural regimes (35-E), D-scout | P0 (items 1–2) |
| 35 | [Strategic-seam refactor](35-strategic-seam-refactor.md) | PHASES A–D LANDED 2026-07-27 (dead-code sweep −3,295 lines; primitives owns its constants, EconomicConstants deleted; economy/ids.ts + censusLens id lenses; propose helpers, optional run, INFRA_ROLES killed, demandLadder, SpawnAnchoredCorp, regimes lens home; was numbered 32 in the branch's commit history — renumbered on merge, master's 32 is graceful-mining-backoff) — open: D-scout, E structural regimes, F legacy-pair port; G flow collapse + H giant splits LANDED | P0 |
| 40 | [Operator contract + variance](40-operator-contract-variance.md) | **Part B LANDED 2026-08-02** (income-statement program phase 2): `telemetry/dutyHistogram` buckets per-sub-window duty with a bimodal verdict, wired into the upgrade meter's per-tick tally (histogram survives the window roll); CarryCorp/ExtensionTender wiring + the H1/X1 percentile columns are the follow-on. Part A (four-term I/O contract, F1 decomposed per-operator) wants spec 39's fleet ownership first; the phase-2 `produced`/`delivered` corps-segment counters (v14) are its measured input side, already differencing per commission | P1 |
| 43 | [Hauler relay](43-hauler-relay.md) | BACKLOG 2026-08-03 (owner design, measurement-gated): bucket-brigade segmentation on long routes - per-creep round trip falls to (2d+2)/N so eol-tail recycles shrink proportionally; hub visit rate is CONSERVED (owner correction: the inner hauler visits N x more - congestion relief is second-order variance/crowding only, with a backpressure risk against it); commission untouched (sophistication inside). Justification rested on v29's recycled-why eol-tail share — **MEASURED THIN 2026-08-04**: eol-tail is 1–6% of a recycled base that nets 0.19 e/t at a 96% recovery rate (M10), so the relay's addressable loss is ~0.1 e/t. PARKED on the owner's own gate ("may be worth it if... a significant energy and spawn parts cost" — it is not); deposit-port expansion still sequences first if revisited | P4 (parked, measured) |
| 44 | [The standing scavenger](44-standing-scavenger.md) | DESIGN 2026-08-04 (owner direction, explicitly not to be rushed): recovery fleet becomes STANDING - a worth-it scavenger is kept, not recycled per pile; sized from the total ground-pile census recalled once per ~1500t (one sizing per body generation, so pile flicker can never churn bodies); focus-fire dispatch because the engine decay floor is ceil(amount/1000) - EVERY pile pays >= 1 e/t however small, so draining one to zero retires its whole floor while skimming many retires nothing. **ALL THREE MEASUREMENT LEGS LANDED 2026-08-05** (census v31 reading steadily: floor share stable ~4 e/t of the L1 line, 3.9-4.9 small piles standing; recovery P&L baseline accumulated through FY4852-M05: net −0.27..−0.39 e/t — the transient cure costs more than it recovers; demob share baselined). Design is measurement-ready; waits on the owner's go. Sizing across heterogeneous piles is the acknowledged hard part - open by design. L1's OTHER half (mouth piles, proportional-bound) belongs to spec 45, not this one | P1 (unblocked, owner-gated) |
| 45 | [Arrivals-first link sequencing](45-link-sink-sequencing.md) | **MEASURED DIAGNOSIS 2026-08-05 (owner-directed); SIZING LEG SHIPPED same day** (feeder volley-service floor: volleyServiceCarry()=16 in primitives, corp + pricing twins together, deployed — body lands at incumbent EOL, prediction registered); sequencing legs = next session's first work item. The sink wait behind the account's two biggest unfavorable lines (~8-10 e/t: pile decay at held mouths + forgone mining) is a SEQUENCING failure, not capacity: coreEmptyShare 0.26 / hubClampShare 0.50 / hubVolleyAvg 378 of 800 measured; ports at ~2x paper headroom (57/61.5 e/t ceilings vs 31/30 assigned) yet PORT B caught live at 800/800 with haulers queued; the feeder's load direction stages bank energy in the core exactly when ports hold volleys; deposit-route bodies carry 978-1,851e into an 800-cap link (2-3 volley cycles standing per trip). Fix order, no new constants: (1) LinkRunner - loaded off-cooldown links outrank the core relay for CTRL's free space (direct-first); (2) feeder becomes a true SERVICE creep (owner sizing doctrine 2026-08-05: drain "pretty much on demand... it can't be a bottleneck; idle haulers are the waste, they're sized for full time moving") - load gate arrivals-first, PRE-drain on imminent volleys, and body floors at one full volley (16 CARRY) with inbound senders: the live feeder measured 4C/200e vs 800e volleys every ~7t - quantitatively the bottleneck, sized by parkedRelayCarry to average flow with no latency requirement; (3) deposit-route hauler bodies cap at 16 CARRY (the landing quantum); (4) re-measure on five named gauges, subscription margin only as last resort. Supersedes spec 26's P7 residue seams with measurement; unblocks the DEP program (~870 tile-e/t) | **P0 (go next)** |
| 39 | [The plan owns the fleet](39-plan-owns-the-fleet.md) | **PHASE 4 (accounting half) SHIPPED 2026-08-06**: auxiliary corps carry their own budget. `infraSpawnLoad` decomposed into three per-corp primitives it now composes (`roomReserverSpawnLoad`/`tenderSpawnLoad`/`feederSpawnLoad`); reservation, tender and controllerFeeder each declare their share; `SIGMA(auxiliary consumes) === infraSpawnLoad` pinned to 1e-12 (exact because reserver load is linear in parts). Found a double-book bug in the doing: the depot movers are COMMISSIONED per spawn room but PRICED per depot room, so a pre-storage room commissioned a tender the ledger never charged for - closed with a `depotRooms`/`linkFedRooms` host lens. Still outside both books: scout/raidGuard/coreBuster/claim. PHASES 0-1 SHIPPED 2026-08-03 (cop ratchet at 11 demand files + 2 spawn sites; solver-backed commissions declare `fleet` per role — the price decomposed, Sigma(load)==spawnPartsPerTick pinned 1e-9 — published on segment 4 v15 next to the actual body, F2 ledger gauge joins them per commission). Phases 2-5 BACKLOG (owner: "corps get their creeps from the plan... corps don't really know anything about spawning, they just know which creeps they have assigned") — the structural answer to 38's P-F (the plan is ADVISORY): invert AUTHORSHIP, not add a gate. The GOAL plan declares the fleet, the NOW plan schedules it, SpawningCorp executes + transfers ownership. Accounting prize: per-commission plan-vs-actual (F2, live), and P4's 7x-under-count class becomes unrepresentable. Hard parts named: actuals must reach the PURE planner via the adapter seam (phase 2), replacement timing needs TTLs, and phase 3's prerequisite spec 36 item 1 (event-triggered replan) SHIPPED 2026-08-03 | P1 |
| 38 | [One bank-drain rate](38-one-bank-drain-rate.md) | **PHASES A+B SHIPPED 2026-08-03** (was: THREE live drain rates, chain out-drew the plan by +15 structurally, 0.220 parts/t unbudgeted). Phase A: `controllerFloorRate` as the controller SINK RESERVE (the floor moved inside the plan, reserve pre-pass wins it first — deployed t72743103). Phase B: `feederRelayTarget` = plan allocation + headroom in EVERY regime; `feederBodyRate` + constructionAbsorb netting retired; `Memory.controllerAllocations` published per room, resolved via `bank.plannedControllerFlow` (ConstructionCorp trunk judge switched). The t72455355 override precondition is impossible by construction — STAGED conformance suite green (bank.test.ts: dry ledger + full bank → floor holds, chain agrees; healthy ledger → full drain flows THROUGH the plan). **PHASE C SHIPPED 2026-08-03/04** (owner: "approach the equilibrium asymptotically"): `bankRefillRate` = deficit/SURPLUS_DRAIN_TICKS mirrors the drain, claimed via the storage sink's reserve + the hub draw-out shrink; `controllerRoutingCapacity`'s filling step RETIRED (spec-33 wartime relegation kept). Live acceptance MEASURED M09–M10: controller budget-vs-actual **+7%** (was +134% under the 15-cap artifact), the budget line breathes (15 → 43.7–55.0), zero swing events. **PHASE D SHIPPED 2026-08-04** (owner: "The bank should be the income mop up not the upgrade"): the residual claimant INVERTED — `bankFedControllerRate` = floor + surplus/τ is the storage-room controller cap (one formula, continuous in the bank level, both 08-03/08-04 rulings hold simultaneously); the bank absorbs every income shock by construction; phase C's refill-claim machinery retired same-day on M10's evidence (unbudgeted burns ate the claim, 76k→27.5k through the target). Predictions registered: allocation →floor now, bank regains target in ~1,200–1,500t, score dips 1–2 months (accepted), then asymptotic climb | P1 |
| 37 | [Builder revitalization](37-builder-revitalization.md) | BACKLOG 2026-07-30 (owner: "tiny builders... not getting fed energy appropriately") — a PROBLEM INVENTORY, deliberately not a design: the fuel lens ignores geometry (`buildFuelPos` surplus branch always wins; builder starved beside a 4,263 pile in its own site room), one wrong distance (100 tiles) deletes WORK via the buffer term (2W/146C measured), fill-toggle deadlock class latent, four notions of "where", maintenance moonlighting. Three same-day fixes landed as guards (reach bound, last-builder rule, tower dead point); the lens is the disease. Owner symmetry thesis: upgrading answers where-is-fuel structurally (0 decision sites), construction re-answers it 22 times. Dedicated session; measurement traps documented in-spec | P1 |
| 46 | [Monthly plan cadence](46-monthly-plan-cadence.md) | DESIGN 2026-08-05 (owner: "we take the budget/plan and we use that for the next fiscal month... resolve along the way and identify large variances as signals to adapt... setting the plan solving from 50 to 1500 effectively") — the month's solve FREEZES as the fiscal month's budget (funded set, allocations, valves, commissions); shadow solves publish nothing and their divergence becomes the adapt signal; early re-solve only on stamped triggers (hostile defund immediate, sustained >±30% income variance, danger floors, console force). Kills the tranche-boundary flap class (d017 measured flapping funded↔over-budget across consecutive solves) and gives every fiscal close ONE budget to measure against. Phase A (cadence+triggers) implementable now; B budget object, C ledger reads it (methodology bump), D shadow-variance row | **P0 (owner-directed)** |

| 47 | [Traffic, the drop-off zone, and the core conduit](47-traffic-and-drop-off.md) | DESIGN 2026-08-05 (owner: "We need something to deal with traffic congestion" — mothership drop-off zone / tractor-beam conduit / layout rebuild / link drop-off). MEASURED ANSWER on file: the LINK half is a confirmed sequencing defect (hubClampShare **0.625** — senders blocked — while coreEmptyShare **0.276** — receiver idle; clamp share risen from spec 45's 0.50 baseline), so "supplement the links with a hauler" would buy bodies around a scheduling bug. The TRAFFIC half is real but splits 4:1 AGAINST the drop-off point (H1 idleSink 0.26 = atSink **0.05** + enRoute **0.21**), so the mothership targets the smaller half. The 21% is NOT localized — spec 14's "instrument, don't re-theorize" applies before building a conduit and certainly before an irreversible layout rebuild. Order: (1) spec 45 sequencing legs, (2) stamp WHERE the delivery leg stalls, (3) build what the data names, (4) structure inventory so the layout question is decidable at all | PARKED 2026-08-06 (behind 45, premise dissolved) |
| 48 | [Gross-vs-net planning basis](48-gross-vs-net-planning-basis.md) | FILED 2026-08-06 (owner: "Why is this using the entire gross energy budget? It should use the net energy budget more like") — MEASUREMENT ONLY, no fix proposed, because the answer changes the shape of the solve and that is the owner's call. Appropriation sinks compete for GROSS mining: the fleet is modelled as a value-100 CONSUMER (44.45 e/t) that also appears in the account as a −38.05 e/t operating cost (one fleet, two books), and losses have a budget of exactly 0.00 — which is why L1 has been the ledger's top line for four cycles at "42x budget": the budget is zero, so any loss is an infinite ratio. Net of both ~42 e/t is appropriable and the plan appropriates 121.5; the books only reconcile because storage silently absorbs the gap at sink value 1. Same defect wearing other hats: P4/P12 over-routing, the bank as permanent residual claimant, E4 idle capital. Cheap reversible first step named (give losses a non-zero budget from the measured meters) | P1 (owner's call) |
| 51 | [The colony budget IS the sum of the corp budgets](51-corp-grained-statements.md) | BACKLOG 2026-08-06 (owner: "Every corp plan is essentially a list of inputs and outputs. Thats the corp budget. The colony budget is the sum of the corps. Each corps is assigned a reporting category for aggregate overview and presentation but the row can be drilled down to the corp level"). **That model is already the `Commission` type** — `{shape, consumes:{energyRate,spawnPartsPerTick}, produces:{energyRate,valuePerTick}, fleet}`, whose own header reads "what the corp consumes... what it produces". The statement is NOT a projection of it: waste-ledger RE-DERIVES the budget column from the flow segment with its own formulas (`minerOverhead`, `haulerOverhead`, and `planSpawnLoad`'s ~120-line reconstruction), so the planner decides a number and the reporting layer independently recomputes what it thinks that number was — a **second book**, and the reason the statement reads "fanciful and deceiving". Three gaps: (a) segment 4 publishes `fleet` but NOT `shape`/`consumes`/`produces`, so the host cannot sum a book it cannot see — cheapest fix, unblocks the rest; (b) **auxiliary corps are OFF-BUDGET (spec 39 phase 4), 7 of 12 live kinds incl. reservation at 19.52 e/t**, so Σ corps ≠ colony budget until 39 lands — the load-bearing dependency; (c) `shape` (4 values) is coarser than the statement's categories, so the KIND should declare its reporting category and `ACCOUNT_CLASS_OF_ROLE` gets deleted (dissolving the tanker/hauler/jack role-mapping defects rather than patching them). Generalizes to the owner's other three — resources, spawn-by-part-type, CPU — as extra columns on the SAME corp rows, since `CommissionInputs` is already a two-resource envelope (CPU blocked: `core.corpCpu` reads **null** in captures). Gated on TIMING: bumps METHODOLOGY, so it lands at a spec-50 sweep CYCLE boundary, which doubles as a free A/B of the re-graining | P1 |
| 49 | [Deposit-port overflow haul](49-deposit-port-overflow-haul.md) | **LEG A SHIPPED 2026-08-06** (owner: "if the link can't keep up to drain the container as fast as all the remotes are filling it up, then we need another haul route... This should be priced into the plan"). Leg A retired the flat `DEPOSIT_PORT_HEADROOM_CAP = 30`, which was measurably binding on BOTH live ports at t72819265 — plan routed exactly 30.00 to each while their links fire 57.14 / 61.54 at rho 0.70 / 0.65, refusing **38.68 e/t** of deposit flow onto long routes costing ~30 CARRY parts per 10 e/t against ~11 via the port, in a colony spawn-bound at 0.91x the ceiling with 23.5 e/t already forgone. The constant survives only as the unknown-geometry fallback; the feeder's hand-copied `PER_LINK_SOURCE_DRAIN = 10 + 30` twin (with its "keep in sync by hand" docblock) now derives from the same primitive per link. **Leg B BUILT AND BACKED OUT same day** — blocked on an EXECUTOR, not economics: the pooled port→hub route can only be attributed to the port's `drainSourceId`, which is link-served BY DEFINITION, and link-served sources get `routes = []` in commissionsFromPlan and are skipped by publishRoster — so it would have been charged to the parts ledger and never fielded, the exact plan-vs-runtime failure the spec exists to prevent. Needs a link-container pickup id (PortRelayCorp, blocked on spec 39). Economics are a TIE not a saving (triangle inequality: `dDirect <= dPort + dOverflow` always; the two terms that appear to tip it are a modelling artifact — effectiveLife models the spawn→post commute, unchanged by a shorter delivery leg — and missing pavement data), so the admission rule is fidelity-justified `<=`, not `<`. INERT on today's geometry regardless: 47.14/51.54 e/t of band against ~40 routed | P1 (leg B blocked on 39) |


| 50 | [The spawn-handicap sweep](50-handicap-sweep.md) | **LANDED 2026-08-06** (owner: "I want to set up an experiment with every fiscal month. We add 1% of handicap. From 0 to 20... I want the bot to set the handicap on its own. I don't want to re-deploy or monitor from here"). The planner's spawn margin walks 0%->20%, one step per fiscal MONTH, then wraps and re-runs — cycling is the METHODOLOGY, not a convenience: a month is a phase sample of the ~9000t bank cycle, so a single pass aliases the bank oscillation against the ramp. The bot escalates 1%->2%/month by itself when its OWN measured controller rate projects RCL 8 landing mid-ramp (measured t72823437: ~97,700t out vs ~31,500t of ramp — a 3x margin, so it stays at 1%). NEVER self-arms: absent `Memory.spawnSweep` resolves to the static 0.9, so the grid/sims/unit suite are untouched and a wiped Memory fails safe to the measured-good value, never to the 1.0 that overheated the colony. Carries **the fiscal archive** (segments 8-9): the bot snapshots its own month boundaries into a Memory-backed ring, so an UNATTENDED period is closeable from one capture at the end — coverage ~100% by construction, where a capture-bracketed close has never been better than approximate. Its round-trip test (archived pair vs full captures, to 0.02 e/t) found four BUDGET-side gaps that would each have silently mis-stated every month of the sweep | P1 |
| 55 | [The spawn fields the planned bodies](55-spawn-fields-the-plan.md) | **STANDING REQUIREMENT — repeatedly asked for, still not met.** Owner 2026-08-08: *"I've been asking for the spawn to field the planned bodies FOREVER."* Not one bug — a PATTERN: four distinct mechanisms found in a single session, each a local heuristic with a good justification and a threshold in the wrong units, sitting between the plan's ask and the purchase. (1) cost-less demand read as gate `"impossible"` for 1804t, port tender never spawned — FIXED at the collection seam; (2) the **mature dead-band** declines the marginal hauler while `deficit < bodyShare/2`, written for "±1 CARRY" jitter but ~10x it at capacity 5600 — **7 of 7 piled sources declined every solve**, fleet at 89% of ask (241 of 271.5 CARRY), shortfall perfectly rank-correlated with the piles, cost **19.48 e/t** of pile decay = 17% of funded capacity; (3) swarm cap denominated in COUNT while the ask stops on CARRY — fixed 2026-08-02; (4) the upgrader `demand: "sliver"`, F2 worst line -35p. Colony-wide F2 deficit **604 declared vs 517 fielded**. Owner's proposed design basis: fold the drain into the SOURCE RATE (`rate += staged/2 / CREEP_LIFETIME`) rather than bolting carry onto a route — one auditable number, every downstream term reprices itself, self-retiring as the pile clears. **Read §5 before patching:** the dead-band cannot be one-sidedly lowered — it exists because of the t72773737 treadmill on d01f, one of the very sources it now starves, and the ask gate and recycle POUNCE share the `worthABody` predicate, so loosening one side re-creates buy-then-cull churn. Acceptance is an F2==0 grid cell (does not exist today) plus an anti-treadmill X5 pin that must fail on a one-sided fix | **P0** |
| 54 | [The link corp owns the link network](54-link-corp.md) | **SHIPPED (core) 2026-08-08, LIVE-UNVERIFIED** (owner: "real LinkCorp that owns the whole link network - core, controller, and ports, with the feeder's link duties moving into it"). Started from a measured dead end: the deposit port's BUFFER container (spec 49) stood at **2000/2000** while `portFallbacks` read 0 on all 8 port-routed routes and `portWaits` ran to 602 - both of a hauler's escape hatches shut, so it queued. Nothing drained it, because the feeder operates the CORE and CONTROLLER links and the only code that transfers INTO a link is `HarvestCorp`; the adjacent-source gate had been dropped on the reasoning that the feeder "staffs it regardless", which was never true of a PORT link. Live geometry makes source-less ports the normal case (both sit 7 and 6 tiles from the nearest source - edge links at range 17 / ~15.5). A standalone `PortTenderCorp` shipped first and lasted ONE commit: it had to take a spec-39 debt entry, and link+tender+container are one machine (mouth, throat, pipe) that only broke because nothing owned the middle. **It is an ABSORB, not a duty move** - `runLinkRouter` is called from inside the feeder CREEP's loop, so the feeder creep IS the link operator and splitting duties would have meant a second body on the heartbeat. So `ControllerFeederCorp` BECAME `LinkCorp`: two roles (walking 1:1 feeder, parked CARRY-heavy port tender), ONE demand site (`feederDemands + portDemands`, retiring the debt entry rather than defeating the cop), ONE price (`feederSpawnLoad + portTenderSpawnLoad` per ported room, `SIGMA(auxiliary corps) === infraSpawnLoad` still pinned), and port work running BEFORE the feeder's gates so the heartbeat can never be slowed by port work. **The kind string is FROZEN** (`controllerFeeder` + legacy nodeId) - CLAUDE.md's rename-orphans-creeps trap - a deliberate code/wire split that is itself open item 3. Also reclaims the dead controller container that was BLOCKING the second port at chebyshev 2 (owner: "the controller link should not have a container"; the `full` gate meant it never fired at 4/5 slots), spill accepted per owner. Full trio green. **Open: live verification is the whole thing (P7 + coreEmptyShare outrank every gain here), the auxiliary-spawn seam is spec 39's and closing it retires 5 debt entries, port (43,38) has no buffer until the reclaim runs, no duty meter on the port role** | **P0 (verify live)** |
| 53 | [Concentration of force: the RCL8 sprint, priced](53-concentration-of-force.md) | BACKLOG 2026-08-06 (owner: "all the rooms in the colony coordinate to quickly build one room up to RCL8... focusing all my rooms on global objectives"). The STRATEGY is not new — GRAND_STRATEGY §4 already names the concentration engine and picks this payload; the spec supplies the arithmetic. **Corrected two numbers in doing so: `CONTROLLER_LEVELS[7]` is 10,935,000 not 7,290,000** (the strategy doc had the last step at ×2, breaking the ×3 run; our own captures settle it — every RCL7 fixture reads `rclProgressTotal: 10935000`), so RCL1→8 is 16,380,200 and every sprint estimate was ~50% short; and **GRAND_STRATEGY §2's free-upgrade-boost claim now needs re-verification** (V1) since it sits in the same doc as the error and, if true, HALVES the whole climb — the largest lever in the spec by an order of magnitude. Core argument: spawn energy capacity is the one resource that does not pool (RCL6 2,300 / RCL7 5,600 / RCL8 12,900 max single creep), and the GCL curve makes concentration near-mandatory — holding 5 rooms requires 27.9M of controller spend while 5 rooms at RCL6 costs only 9M, so 18.9M is forced somewhere and the only choice is its SHAPE (5×RCL7 vs 1×RCL8+4×RCL6, identical cost and GCL, income within 10%). Income statement: 62.3% of gross harvest reaches the controller; transport is only 5.8% (so concentration is cheap), hauling is the largest line at 9.2%, and **reservation at 10.6 e/t costs more than the terminal tax** (a CLAIM part is 1 e/t permanently — currently unnamed). Explicitly NOT justified militarily — GRAND_STRATEGY §5/§6 hold, this out-grows rather than out-fights. Gated on V1 (boost) + M1 (income statement as a measured ledger ratio, not a spreadsheet) | P2 |

Recently completed (for context): economy consolidation onto
`economy/primitives` + CorpPlanner (FlowSolver deleted); storage-as-core-depot
at RCL4 with the `storage-depot` integration test; the free-economy mod fix
(CONSTRUCTION_COST 0 made building impossible, now 1); the
reserve-source-only-with-a-fielded-builder gate.
| [41](41-fiscal-periods.md) | Fiscal periods, the fiscal close, and the standing report set | LANDED 2026-08-01 |
| 42 | [The energy controller budget](42-energy-controller-budget.md) | BACKLOG 2026-08-01 (owner: "describe the end result of this energy controller budget we're working on") — the DESTINATION for specs 14/15/20/38/39/40/41, which describe mechanisms built one incident at a time. Four properties define done: every line has a BUDGET not just an actual; the residual closes to ~0 because every joule has a named home; variances split cleanly into plan-accounting vs runtime-behaviour; and the numbers survive comparison over time. Prices the gap: ~60 e/t of 100 was measured-but-unpriced at proposal (pile decay 15.67, tombstones 12.21, forgone mining 30.28, structure decay 4.26, link throughput ceiling), which is why the controller allocation and the measured score had never agreed. Ships in four independently-useful stages: A every loss gets a budget, B the residual closes, C budget equals actual (needs P12 unified + spec 38), D the budget becomes a control surface. **PROGRESS 2026-08-05 (methodology #11): stages A-B substantially real — the budget column balances by construction (bank = plan residual), the bridge closes to +0.00, the residual runs ±0.5-6.6 e/t on recent windows vs the 14%-of-gross first baseline; P12 reads 1.0x at the phase-D law. Remaining to C: losses still enter budget at trailing actuals not priced budgets (L1's 0-budget pile line is the gap), the 3.34 e/t parts-vs-energy internal gap, and spec 45's sink-wait so delivered meets capacity** | P1 |
